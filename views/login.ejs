<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login | Ai-Arrhythmia Prediction</title>
    <link rel="stylesheet" href="/styles.css" />
    <script src="https://unpkg.com/lucide@latest" defer></script>
  </head>
  <body>
    <main class="page page-auth">
      <header class="app-header auth-header">
        <div class="topbar">
          <div class="topbar-main">
            <h1>Login</h1>
            <p>Sign in to continue as admin, doctor, or patient.</p>
          </div>
          <div class="topbar-actions">
            <a href="/register" class="ghost-link"><i data-lucide="user-plus" class="icon"></i>Register</a>
          </div>
        </div>
      </header>
      <section class="panel auth-panel">
        <% if (error) { %>
          <div class="alert alert-error"><%= error %></div>
        <% } %>
        <form method="POST" action="/login" class="auth-form">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
          <label class="field">
            <span>Username</span>
            <input type="text" name="username" value="<%= username %>" required />
          </label>
          <label class="field">
            <span>Password</span>
            <input type="password" name="password" required />
          </label>

          <button type="submit" class="submit"><i data-lucide="log-in" class="icon"></i>Login</button>
        </form>

        <p class="muted-note">New user? <a href="/register" class="auth-link">Create an account</a></p>

        <div class="demo-credentials">
          <h2>Demo Accounts</h2>
          <ul>
            <li>admin / admin123</li>
            <li>doctor / doctor123</li>
            <li>patient / patient123</li>
          </ul>
        </div>
      </section>
    </main>
    <script>
      window.addEventListener('DOMContentLoaded', function () {
        if (window.lucide && typeof window.lucide.createIcons === 'function') {
          window.lucide.createIcons();
        }
      });
    </script>
  </body>
</html>
